name: build_github_page 
on:
  push:
    branches:
      - main
job:
  deploy_mkdocs:
    runs-on: ubuntu-latest
    steps:
      uses: action/setup-python@v4
      with:
        python-version: 3.x
      run: pip install mkdocs-material
      run: mkdocs gh-deploy --force
